// Acceso cerrado: el navegador no habla con Firestore.
// Toda lectura/escritura va por firebase-admin en el servidor.
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
